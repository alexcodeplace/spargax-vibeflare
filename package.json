{
  "name": "vibeflare",
  "version": "0.9.6",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @vibeflare/worker dev",
    "dev:ui": "pnpm --filter @vibeflare/ui dev",
    "build": "pnpm build:cloudflare",
    "test": "pnpm -r test",
    "deploy": "pnpm db:migrate:cloudflare && pnpm --filter @vibeflare/worker exec wrangler deploy --config ../../wrangler.toml",
    "cli:build": "pnpm --filter @vibeflare/cli build",
    "validate:wrangler": "bun tools/validate-generated-wrangler.ts",
    "release:gate": "pnpm build:all && pnpm validate:wrangler && pnpm validate:deploy-button && pnpm security:audit && pnpm notices:check && pnpm test && pnpm --filter @vibeflare/ui test:e2e",
    "security:audit": "pnpm audit --prod --audit-level=high",
    "release:verify": "bash tools/run-release-verification.sh",
    "release:archive": "bash tools/build-release-archive.sh",
    "notices:generate": "node tools/third-party-notices.mjs",
    "notices:check": "node tools/third-party-notices.mjs --check",
    "deploy:cli": "pnpm cli:build && ./apps/cli/bin/vf deploy",
    "db:migrate:cloudflare": "pnpm --filter @vibeflare/worker exec wrangler d1 migrations apply DB --remote --config ../../wrangler.toml",
    "validate:deploy-button": "bun tools/validate-deploy-button.ts && pnpm --filter @vibeflare/worker exec wrangler deploy --config ../../wrangler.toml --dry-run --outdir ../../.wrangler/deploy-button-dry-run",
    "build:cloudflare": "pnpm --filter @vibeflare/worker build && pnpm --filter @vibeflare/ui build",
    "build:all": "pnpm -r build",
    "design-assets:check": "node tools/sync-club-design-assets.mjs --check",
    "logo-assets:check": "node tools/verify-logo-assets.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  },
  "packageManager": "pnpm@11.24.0",
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  }
}
