#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BIN="$ROOT/apps/cli/bin/vf"
if [ ! -x "$BIN" ]; then
  printf 'vf is not built yet. Run ./install.sh first.\n' >&2
  exit 1
fi
exec "$BIN" "$@"
