# VibeFlare v0.9.6 deployment and verification

Deployed to https://vibeflare.shrill-moon-94bd.workers.dev on 2026-09-13.

## Delivered changes

Owned image, embedding and audio conversations now appear immediately in Workspace and reopen from History with saved results. Audio uploads use the correct Whisper/Turbo/Nova-3 interfaces; live-only Deepgram Flux is excluded from the upload picker and rejected before inference. The four task tabs share consistent panel geometry and accessible file drop/browse controls. Embeddings explain semantic search and provide complete JSON downloads. Text drops are actual model input, not silently ignored attachments.

See [media-history.md](../media-history.md) and [task-workspace.md](../task-workspace.md) for behavior, retention, compatibility and provider references. Existing visible controls and personal model visibility/Settings deep links are retained. The concurrently completed README refresh was integrated and all eight screenshots were recaptured against the v0.9.6 local build, with hashes and source provenance.

## Exact deployment

- Worker version: `4426bcf0-cbe2-4e8b-9d6b-63bc79f7d44c`.
- Deployment: `2c1a25a0-698b-43e7-9458-14cc9fd47e51` at `2026-09-13T11:33:40.288738Z`, serving 100% of traffic.
- Uploaded application code: `8f1b2c6a0d948926ca9fe18340f15eb23fd02011`. Later commits only updated tests, screenshots and documentation/tooling; the deployed UI/Worker/shared application tree is unchanged.
- Previous healthy version retained for rollback: `dfcfafb3-d095-44b0-89d2-b58b4cef894b`.

The version was first staged at zero ordinary traffic. The first immediate static read still returned the previous HTML; later exact-build checks passed without changing or weakening the validator. Promotion happened only after the complete local browser gate passed. Ordinary no-override live checks then passed, including health v0.9.6, root routing, existing auth methods, five HTML page hashes, 19 linked runtime assets and desktop/mobile light/dark sign-in rendering. Before/after provider metadata confirms unchanged resource identities, variables, compatibility, observability, schedules and workers.dev/preview settings.

No production migrations, account creation, inference calls or data resets were performed by this deployment. Authenticated model flows were tested against isolated local D1/R2/DO fixtures, not by impersonating a production member.

## Verification

- 153 Worker unit tests passed.
- 68 UI unit tests passed, including the 15 newly integrated screenshot-target safety cases.
- 26 CLI tests passed.
- 103 browser tests passed; zero failures, retries, flaky or skipped tests. The final run did not update snapshots.
- Production UI build generated all 17 routes. Worker types, Astro diagnostics, island-hydration audit, workflow contract and both generated Wrangler configurations passed.
- Production dependency audit reported no known vulnerabilities. All 128 Club assets match the canonical manifest.
- Eight intentionally changed chat/files baselines were visually reviewed before acceptance. Gallery-control measurement now waits for its own React island instead of measuring a detached server-rendered node.
- Eight current README images and their version/source/checksum manifest passed verification.

GitHub's self-hosted PR gate has no registered runner and did not execute. Its queued status is not counted as a successful CI run; the executable local test receipts above are the release evidence. No required checks or branch protections were disabled.

Machine-readable receipts, exact test logs, screenshots and private before/after provider metadata are preserved under the VM's private archive: `/home/user/Projects/.archives/vibeflare-branch-cleanup-20260913/`. Old uncommitted source files are backed up and retained rather than discarded during merged-branch cleanup.
